Simple plan — finish D-PR6 → land deferred D residuals → execute Wave E
Three phases, ordered. Each step has: scope, files, validation, ship gate.

Phase 1 — Close D-PR6 (1 publish)
Goal: clear remaining D-PR6 sub-PRs, get cron_drift to 0 in prod, get the test suite to 100% green.
#	Task	Files	Done when
1.1	Sub-PR 8 cluster A (3 tests) — advance seeded claim's phase past triage (submitted + response_received), stamp submitted_via, before PATCH outcome. Template: duplicate-of-endpoints.test.ts::seedGroup.	__tests__/closure-data-foundation.test.ts (3 cases)	3 PATCH outcome tests pass.
1.2	Sub-PR 8 cluster B (1 test) — widen the "payor portal response" regex OR update the assertion to match validateDeniedRequiresResponse's actual phrasing.	__tests__/closure-data-foundation.test.ts (1 case), read routes/claims.ts + helper for current text	Regex matches; test passes.
1.3	Sub-PR 8 cluster C (1 test) — claim-move trigger violation. Stamp non-unclassified disposition + matching phase on seeded claim, OR keep destination group at phase='triage' until after move.	__tests__/closure-data-foundation.test.ts (1 case)	validate_disposition_against_phase no longer fires; test passes.
1.4	Sub-PR 5 — §3.E status aggregates → phase. Read each consumer site first (per D-PR6 risk note: don't flatten display-axis aggregates). Anything that feeds a chart/table broken out by status name: leave on status with inline // display axis comment.	routes/dashboard.ts, routes/insights.tsx data path, anywhere flagged in D handoff §3.E	Predicates flipped where safe; flagged sites carry comment + TODO(Wave E).
1.5	Sub-PR 7 — trivial vocab-cleanup backfills (per D-PR6 handoff line 83).	scripts/oneshot-*.ts cleanup pass	Scripts run dry, then live; audit log entry per script.
1.6	Skip Sub-PR 4 (9-row dual-terminal backfill). The §G fingerprint update from Sub-PR 1 already handles it; dropping to 0 needs no data change. Document the skip in catalogue.	—	Decision logged.
1.7	Validation gauntlet — composite tsc -b lib/db lib/vocab lib/leg-state lib/invoice-state → api-server tsc --noEmit → pnpm --filter @workspace/api-server test (TZ=NY) → schema-drift workflow → check:invoice-state-derivation.	—	All green. Target: 740/740 tests pass.
1.8	Publish #3.5b → prod. Bundles cron Option B writer (Sub-PR 2/2.b), Sub-PR 5, Sub-PR 7, Sub-PR 8 fixes.	—	Prod publishes; SSE clients reconnect.
1.9	Post-publish prod verification: re-run §G fingerprint → confirm cron_drift = 0. Run check:invoice-state-derivation against prod → confirm zero violations.	—	Prod fingerprint shows 0 across the board (prod-fixture noise OK).
Ship gate to Phase 2: Prod fingerprint clean + 24h soak.

Phase 2 — Land the 4 D residuals that defer into E (1 publish)
These are in state-wave-d-handoff.md §3.B but not in §8 of the execution plan. They have to land before the DROP migration.
			
			
			
			
			
			
			
Ship gate to Phase 3: The Wave E precondition. Conformance script (deriver-vs-stored) returns 0 disagreements in prod for ≥72h continuous. This is the gate D handoff §3.B specified and §8 is missing.

Phase 3 — Wave E (1 publish, ~2-3h active work)
Execute §8 of the execution plan, in order, with the additions §8 misses.
			
			
			
			
			
			
			
			
			
			
			
			
			
			
Calendar
			
			
			
			
Total: ~11h active, ~5 calendar days (vs. the original plan's 7h+7d for D→E).

Risks to watch
* Phase 1.4 (Sub-PR 5): easy to flatten a display-axis aggregate. Read each consumer before flipping; leave with comment + TODO if it feeds a status-named chart.
* Phase 2.1: GENERATED column needs a backfill window — existing rows compute on first read, but ensure no migration races with live writes (run during low-traffic).
* Phase 2.5: deriver fix has to land BEFORE the 72h soak starts; otherwise the conformance count is non-zero and Phase 3 is blocked.
* Phase 3.3: the DROP migration is the only truly irreversible step in the entire refactor. Take a snapshot immediately before; verify Phase 2.6 conformance is still 0 the morning of the publish, not just at the start of the soak.
* Phase 3.12: don't skip the stale-tab broadcast. Without it, any operator with a tab open from before the publish will silently get undefined in places that read event.status / event.outcome.

Want me to (a) start executing Phase 1 now, (b) write this plan into a file (e.g. docs/architecture/state-wave-de-finish-plan.md), or (c) hand off any of the three phases as a project task?
