# ClaimClear Project

## Overview
ClaimClear is a comprehensive NEMT (Non-Emergency Medical Transportation) rejected claims dispute tracker platform. Its main goal is to optimize the claims dispute process, minimize financial losses, and enhance operational efficiency for NEMT providers. It achieves this by enabling the import of rejected claims, guiding staff through decision-tree workflows for dispute resolution, and automating dispute submissions to the MAS Transportation Provider Support Portal. The project aims to significantly reduce the administrative burden associated with claim disputes and improve recovery rates for NEMT services.

## User Preferences
I want to emphasize iterative development and prefer detailed explanations when new features are introduced or significant changes are made. I appreciate clear, concise communication and enjoy seeing functional programming paradigms where they enhance code readability and maintainability. Please ask before making any major architectural changes or introducing new external dependencies.

## System Architecture
The project is structured as a pnpm workspace monorepo utilizing TypeScript, designed for scalability and maintainability.

**Technology Stack:**
-   **Monorepo:** pnpm workspaces
-   **Backend:** Node.js 24, Express 5, PostgreSQL, Drizzle ORM, Zod
-   **Frontend:** React, Vite, Tailwind CSS, shadcn/ui
-   **Authentication:** Replit Auth (OpenID Connect with PKCE)
-   **AI:** Anthropic Claude
-   **Bot Automation:** Playwright
-   **API Codegen:** Orval

**Core Architectural Decisions:**
- **Monorepo Structure:** Uses TypeScript composite projects and pnpm workspaces for type safety and dependency management.
- **UI/UX Design:** Adheres to an Agape brand color scheme (dark navy, blue, orange, gold) with a distinct logo, and utilizes densified detail surfaces for improved information display.
- **Centralized Error Handling:** Robust error handling for async Express routes to prevent information leakage.
- **Database Design:** PostgreSQL with Drizzle ORM, featuring 16 entities for comprehensive claim and user management, and optimized with database indexes.
- **Authentication:** Session-based authentication supporting general users, admins, and bot tokens.
- **Frontend Serving:** React + Vite frontend served as static files by the Express API server from a single port.
- **Claim Workflow & Statuses:** Claims progress through predefined statuses with centralized transition functions to ensure consistent audit logging, timeline notes, and real-time SSE events. A dedicated "Non-Issue" outcome handles claims requiring classification.
- **Invoice Grouping:** Claims are grouped by invoice number, forming the primary unit for dispute resolution, with group-level statuses, outcomes, and evidence tracking.
- **Response Tracking & Classification:** Tracks incoming responses, auto-matching them to claims using a tiered approach. A classifier pipeline uses phrase-signature matching, falling back to Anthropic Claude for AI-powered classification.
- **Per-Leg/Per-Invoice State Machine:** Uses discrete, typed columns for managing `claims` and `invoice_groups` state, with leg sub-status always derived.
- **Included-in-Dispute Management:** Explicit transitions and importer defaults manage `claims.included_in_dispute`, allowing for clear exclusion/inclusion of legs from disputes.
- **Per-Leg investigation UI:** Dedicated UI for detailed per-leg investigation, including the SOP-advance player, the inline `PerLegContextEditor` (with AI clarification gate), and hold/reclassify actions. The editor renders below the question card on every walk step AND on the inline "Ready" surface when a leg lands at an include outcome — there is no separate end-of-walk "I'm done — hand off" screen, because the leg already lands in terminal state via the prior `/sop-advance` POST. Closed/Hold/Duplicate keep their dedicated terminal screens (each captures outcome-specific information). Per-leg context capture is fully optional; the AI clarification gate (POST `/api/claims/:id/per-leg-context-readback` → review → POST `/api/claims/:id/per-leg-context`) mirrors the group-submission readback. Legacy auto-derived "• Q — A" bullet-list `perLegContext` values are treated as empty by the editor (the SOP transcript card on the leg page still surfaces them so nothing is lost). Status changes are driven exclusively by gauntlet's generate-preview / submit-now path — there is no operator-driven "package" CTA or `/invoice-groups/:id/package` endpoint.
- **Portal Submission Readiness Gates:** Enforces ordered readiness gates for operator-initiated submissions, with a system/bot actor path bypassing some gates.
- **Per-Leg Hold (Partial Submissions):** Allows individual legs within a multi-claim invoice group to be put on hold, enabling partial submissions.
- **Post-Response Workflow:** Claims enter a "Needs Review" state after a payor response, prompting staff to choose actions.
- **Decision Trees (SOP Logic):** Integrated workflow system for SOP logic, evidence collection, branching, and dispute reasons, with an "SOP-Hold Escape Hatch" to clear holds and resume workflows.
- **Error Type Model:** Simplified model for error types, including a "Submission Path" picker to determine the dispute submission method.
- **Evidence Management:** Supports object storage for evidence files, linked to decision tree nodes and collected for portal submissions.
- **Real-time Updates:** Server-Sent Events (SSE) provide real-time updates and collision detection.
- **API Security:** Role-based authentication middleware protects API routes.
- **Cron Jobs:** Scheduled jobs for processing portal submissions, daily briefs, and payor response scanning.
- **Portal Worker (on-demand):** In-process Playwright bot for MAS Portal interaction, launching a fresh browser for each invocation. **Group-atomic end-to-end (Task #485):** `portal_submissions` rows are one per invoice group (not per leg) and carry a `legs` JSONB column (`{ legId, confNumber, ticked, error? }[]`) populated at draft time and overwritten by the producer with the worker's `perLeg[]` outcomes. The list page renders one row per group with an "N legs" pill; the drawer reads `legs[]` directly to show per-leg ticked/error state. Legacy per-leg rows (pre-Task #485) keep `legs == []` and the drawer falls back to a "details unavailable" notice. Migration `0032_portal_submissions_legs_jsonb.sql` is idempotent and does **no** backfill.
- **System Health Rollup:** Provides a consolidated view of system health, including connector probes, cron freshness, worker status, and overdue submissions.
- **Header Batch Status Pill:** Persistent UI element displaying live queued-claim count, next batch countdown, and in-flight indicators.
- **Summary Analytics Page:** Provides time-range-aware analytics, including activity trends, recovery trends, team productivity, and breakdowns by status/outcome/error types.
- **Repeat Offenders Aggregation:** Aggregates rejection statistics per driver/member, excluding "Non-Issue" outcomes.
- **Schema Drift Guard:** Automated script to prevent silent divergence between Drizzle schema files and generated SQL migrations.
- **Deploy-time DB Migrations:** Explicit, idempotent SQL migration runner (`lib/db/scripts/apply-migrations.mjs`) wired into `.replit`'s `[deployment.build]` pre-build hook so it runs **once per publish attempt, before any artifact-specific build**, in its own clearly-labelled build-log step. Migrations are intentionally **not** chained inside `api-server`'s `production.build` (where they used to live) so a single failed publish only ever advances the schema once and the migration step is greppable in the build log. Note: a failed publish can still leave prod with a forward schema if the migration succeeds but a later step fails — that's the same risk the previous setup had; the change just stops it from being hidden inside the api-server chain. See Task #340.
- **On-Clock Status Tiers (Task #352):** Two reconciled tiers avoid badge contradictions across dashboard/queue/claim surfaces. (1) **Pre-submit urgent** (`GROUP_EXPIRING_ACTIONABLE_STATUSES` = New, Needs Evidence, On Hold, Generating Email; claim-level adds Portal Queued + Processed) — drives `isUrgent`, red "Today" badge, Dashboard "File today" hero, Queue hero count. (2) **Submitted-but-unconfirmed** (`GROUP_SUBMITTED_STUCK_STATUSES` = Portal Queued; `CLAIM_SUBMITTED_STUCK_STATUSES` = Portal Queued + Processed) — drives `submittedStuck`, amber "Stuck" badge, Dashboard "Stuck after submission" hero, Queue `?expiring=stuck` filter. At the **group level** the two sets are disjoint (Portal Queued is pre-submit for claims but post-submit for groups). At the **claim level** submittedStuck is a strict subset of isUrgent. All surfaces use the same date math (effectiveDaysRemaining ≤ 0 = deadline slipped). The backend exports `GROUP_SUBMITTED_STUCK_STATUSES`, `CLAIM_SUBMITTED_STUCK_STATUSES` from `routes/dashboard.ts` and uses them in `lib/expiring-filter.ts` (`ExpiringMode = "soon" | "urgent" | "stuck"`), `routes/invoice-groups.ts`, and `routes/claims.ts`. The dashboard summary exposes `submittedStuckCount` and `submittedStuckGroups` alongside `urgentCount` and `expiringGroups`.
- **Production Build Topology:** Only `artifacts/api-server` is registered in `.replit`'s `[[artifacts]]` list (autoscale deployable). `api-server`'s `production.build` chains `claimclear`'s Vite build because `app.ts:71-80` statically serves both `claimclear/dist/public` (root + SPA catch-all) and `training-guide/dist/public` (mounted at `/training-guide`) at runtime — `api-server`'s own esbuild (`build.mjs`) only bundles the API. The chain also installs Playwright (required at runtime by `bot/batch-worker.ts` / `bot/save-session.ts` for MAS portal automation). `mockup-sandbox` is the Canvas / dev preview server and is intentionally **not** registered in `.replit`'s `[[artifacts]]` list — it has no production role. (`training-guide`'s static dist is currently expected to be present at runtime; if it is not auto-built by the deployer, add it to api-server's build chain or register it as its own deployable — Task #340 deferred this confirmation pending the next publish log.)
- **Tour Version Drift Guard:** Mirrors the schema-drift pattern for the admin tour. `artifacts/claimclear/scripts/check-tour-version.mjs` reads `src/tour/tour-config.ts`, extracts `CURRENT_TOUR_VERSION`, hashes the file with the version line stripped, and compares both against `src/tour/tour-version.lock.json`. Any drift fails the build with an actionable message: changing tour content without bumping `CURRENT_TOUR_VERSION` would silently skip the new tour for every existing user (their `users.tour_version_seen` already matches), so the guard refuses to ship that combination. Wired in three places: (1) the `tour-version-drift` validation workflow in `.replit` (parallel-run under `Project`, `isValidation = true`) so the dev environment surfaces drift on startup, (2) chained into `claimclear`'s `build` script so any production build via `pnpm --filter @workspace/claimclear run build` (which `api-server`'s `production.build` invokes) fails before vite runs, and (3) usable on demand as `pnpm --filter @workspace/claimclear run check-tour-version`. After a deliberate edit, regenerate the lock with `pnpm --filter @workspace/claimclear run check-tour-version -- --update` and commit `tour-config.ts` + `tour-version.lock.json` together.
- **In-App Guided Tour (admin):** React-Joyride v3 driven product tour for the ClaimClear admin app. Steps are declared in `artifacts/claimclear/src/tour/tour-config.ts` (with a `CURRENT_TOUR_VERSION` string and per-step `target` CSS selectors / `route`); `AdminTourProvider` wraps the `AppLayout` in `App.tsx` and handles cross-page navigation (it pauses the run, calls `setLocation` via wouter, polls for the target el, then resumes). Auto-trigger logic: per-user server-side flag `users.tour_version_seen` (added in migration `0026_tour_version_seen.sql`) exposed via `GET/PATCH /api/auth/user/tour-state` (handlers in `artifacts/api-server/src/routes/auth.ts`, schema in `lib/api-spec/openapi.yaml`); when the stored version is missing or != `CURRENT_TOUR_VERSION` the tour starts automatically on first sign-in, and `finish()` PATCHes the new version. Replay anytime via the **sidebar "Help → Take the tour"** entry (`data-tour="sidebar-take-tour"` in `components/layout.tsx`) — that's the primary discovery surface because operators look in the nav for "replay the tour", not at a small header icon. The header **"Take the tour"** button (`data-tour="header-take-tour"`) is still rendered as a backup with its label always visible (no `hidden sm:inline` gating). Bumping `CURRENT_TOUR_VERSION` re-runs the tour for every user once. Targets are wired with `data-tour="…"` attributes (sidebar, page-main, dashboard-kpis, dashboard-today, queue, responses-awaiting-review, attestation-queue, sidebar-take-tour). The previous standalone `artifacts/admin-walkthrough/` video artifact has been retired. **Safety invariant (do not regress):** the DB write that records `users.tour_version_seen = CURRENT_TOUR_VERSION` is gated on `sawAtLeastOneStepRef` (set when joyride emits `EVENTS.TOOLTIP`). `EVENTS.TARGET_NOT_FOUND` quietly closes the tour without writing, and `STATUS.FINISHED`/`STATUS.SKIPPED` only write when at least one tooltip actually rendered. Earlier versions (pre-v4) auto-incremented `stepIndex` on `TARGET_NOT_FOUND` and called `finish()` (which writes) once index walked past the end — that silently marked existing users "seen" without them ever viewing a single step, bricking auto-start permanently. Any rewrite of `handleEvent` in `admin-tour.tsx` must preserve this gate. **Tour structure (v2026-05-04.v4):** 14 steps in two parts — (1) **process orientation** (welcome modal + five "Step N of 5" centered modals establishing the repeatable ClaimClear loop: upload transactions → understand each invoice → gather evidence → submit dispute → read response & decide re-attestation, plus a transition modal), (2) **UI walkthrough** that anchors each screen back to the process step it serves (sidebar, dashboard KPIs, today's hero columns, queue = Steps 3–4, responses = Step 5, attestation queue = closing the loop, and a final "Replay anytime" step pointing at the sidebar Help entry). The orientation prefix exists so brand-new hires understand the *why* and the loop before being shown which buttons live where.
- **Communication Components:** Integrated email thread components for group and leg detail pages, including rich HTML rendering, metadata, and Tiptap-powered rich text reply composer.
- **Endpoint-Action Contract (Task #411):** Every action-named endpoint must produce its named side effect — never just stamp a flag and return 200/201/204 to make the UI feel responsive. Concrete rules: (1) "Bulk *" endpoints AND bulk client-side loops (e.g. queue "Mark all as no-issue") must report per-row `{ succeeded, skipped: [{ id, reason, ref }] }` — generic `{ updated: requestedCount }` or "Marked N legs" toasts are forbidden because they lie when only some rows match. The bulk-no-issue loop in `queue-needs-review-panel.tsx` is the reference implementation: each leg run inside its own try/catch, partial-failure toast lists skipped refs (truncated at 5) and the first error reason. (2) When the operator bypasses a soft block (lint warnings, etc.), write a dedicated audit row whose `action` names the bypass (`lint_warnings_bypassed`) with the bypassed payload AND the operator's typed reason in `metadata` — do NOT fold it into the next generic confirm row, and do NOT accept the bypass without a written reason. The confirm route enforces a min 10-char `bypassReason` server-side and rejects with `400 + code:"missing_bypass_reason"` so the contract is the same for the UI dialog, the bot, and curl. The reason is also surfaced in the `details` string so the activity feed shows it without drilling into metadata. (3) Bot/admin "submit-now" / "process X" paths must refuse with 4xx + a stable machine-readable `code` (e.g. `code:"missing_description"`, `code:"missing_bypass_reason"`) when required inputs are absent — silent template fallbacks (`buildFallbackDescription` and friends) are banned because they ship un-reviewed content downstream. (4) Action endpoints with no UI caller must be removed, not left as dead admin-shaped routes; if a future admin UI needs them, re-add them together with the UI in the same change. (5) Mutating UI affordances must be backed by an actual write call — buttons that "work" by setting local state without persisting it are the exact failure mode this contract exists to prevent. (6) **Refusal paths must not mutate.** Endpoints that gate on phase/preconditions (`/preview-generated`, `/draft/regenerate`, `/reattest/complete`, `/post-response-action`, etc.) MUST NOT stamp their named timestamp/flag column when they refuse with 4xx. The contract test seeds a row, hits the refusal path, and asserts the column is unchanged. (7) **Atomic finalize for two-step uploads.** The `claim_evidence` insert flow finalizes a GCS blob first (PUT `/storage/uploads`), then inserts a row pointing at it (POST `/claims/:id/evidence` or `/claim-evidence/closure`). If the row insert throws, the route MUST best-effort delete the orphan blob via `ObjectStorageService.tryDeleteObjectEntity` — but only if no other `claim_evidence` row already references the same path. The wrapper lives in `routes/claim-evidence.ts` (`bestEffortCleanupOrphanBlob`); reuse it for any future two-step finalize-then-insert flow. (8) **Unsaved-draft navigation guard.** Any in-page editor that holds typed-but-unsaved text (per-leg context, dispute draft, free-text classification notes, etc.) must guard against silent loss on **all three** exit paths: `beforeunload` (tab close / refresh), `history.pushState`/`replaceState` (SPA navigation — wouter and friends call these directly and they don't fire `beforeunload`), and `popstate` (back/forward). The reference implementation is in `per-leg-context-editor.tsx`'s `useEffect` block keyed on `hasUnsavedDraft`. (9) **"Coming soon" continuation paths must self-document.** Where a button still routes to a generic interim state (post-response continuation actions for re-dispute / re-attest / submit-new-invoice currently all land at `Needs Evidence`), the label must include "(workflow coming soon)" AND the click must also create a follow-up tracking note via `POST /api/notes` so the timeline carries an explicit record of which continuation path the operator chose. See `queue-response-review-panel.tsx` `handleContinuation`. The contract is enforced by `artifacts/api-server/src/__tests__/endpoint-action-contract.test.ts`, which seeds real rows, calls each endpoint, and asserts the named side effect (audit row written including bypass reason, row deleted, skipped[] populated, removed routes return 404, refusal paths leave columns untouched). Add a case there before introducing any new action-named endpoint.
- **Group Re-attest Bulk Queue Endpoint:** The Re-attest modal's "Queue for later" path is backed by a single atomic group-level endpoint `POST /api/invoice-groups/:id/reattest/queue` (operationId `bulkQueueGroupReattest`, hook `useBulkQueueGroupReattest`). It bypasses the Task #196 per-leg attestation gate and writes `attestation_state=queued` + `attestation_queued_at` + `attestation_queued_by` directly on every Approved/Partial operator-confirmed leg in the group, stamps `awaiting_payor_again_at` on the group, writes per-leg `attestation_queued` audit + `leg.attestation_queued` state event rows (each tagged `metadata.bulk=true`, `metadata.source=group_reattest_queue`) plus one umbrella `group_reattest_queued_bulk` audit + `group.reattest_queued_bulk` state event — all in one db.transaction. Post-commit it broadcasts SSE updates and refreshes group derived fields. Returns `{ succeeded, skipped }` per the Endpoint-Action Contract; refuses with 404 (missing group), 409 (`group_not_in_needs_review`, `group_missing_inbound_response`, or `no_eligible_legs`). The per-leg `POST /api/claims/:id/attest/queue` route is preserved for the Attestation Queue page; do **not** call it in a client-side loop from the group modal — that path enforces the gate and reverts each leg's `awaiting_payor_again_at` independently. Tests live in `artifacts/api-server/src/__tests__/group-reattest-queue.test.ts`.
- **Micro-interaction Framework (Task #509):** The 9+ ClaimClear UI micro-interactions (day-complete confetti, session milestones, decision-tree checkmark, group-cleared card fade, save breath, bulk row shimmer, copy chirp, row settle, skeleton crossfade, number ticker, rotating captions) share seven framework primitives so future interactions opt into shared plumbing instead of cloning a sibling. Reduced-motion check: `useReducedMotion()` + `prefersReducedMotion()` (`src/hooks/use-reduced-motion.ts`) — the only JS source-of-truth, with CSS-driven animations still gated by `@media (prefers-reduced-motion: reduce)` in `index.css`. One-shot timer + cleanup: `useTransientFlag(durationMs)` and `useTransientFlagSet<Id>(durationMs)` (`src/hooks/use-transient-flag.ts`) — `useBreath`, `useClipboardCopy`, `useRowBreath` are now thin semantic wrappers; the inline `justProcessed`/`justShipped`/`justCleared` `setTimeout` blocks in `claim-detail-v2`, `leg-conclusion-row`, `invoice-group-detail-v2` all delegate. "Actor caused this transition" gate: `useActorCausedTransition` (`src/hooks/use-actor-caused-transition.ts`) owns the prev-ref + mount-skip + same-value-skip + `consumeLocalActionMark` fast path + SSE-author collaborator suppression that used to be copy-pasted in three watcher effects. EventSource lifecycle + exponential backoff (+ optional `replayGuard`): `useEventSource` (`src/hooks/use-event-source.ts`) — replaces the 6 hand-rolled connect functions in `use-claim-events.ts` and `use-system-events.ts`. Confetti hierarchy as the type system, not a comment: `CelebrationTier` + `fireCelebration(tier)` in `src/lib/celebrations.ts` is the **only** import site for `canvas-confetti`; adding a new tier requires editing that file. Toast copy registry: `celebrationCopy(ctx)` in the same module owns the Friday-afternoon day-complete variant and the 10/25/50 session-milestone copy. Full primitive table, reduced-motion contract, and a worked example for adding a new interaction live in `artifacts/claimclear/docs/microinteractions.md`.
- **Bot Service Token Storage:** `BOT_SERVICE_TOKEN` (the shared secret used by api-server's `requireBotToken` / `requireAuthOrBot` middlewares to authenticate internal bot/cron callers, e.g. `bot/batch-worker.ts` and `bot/cron-payor-response-scan.ts`) **must live in Replit Secrets, never in `.replit`'s `[userenv.*]` blocks**. Storing it in `.replit` puts it in source control / git history, which is how the original token leaked (Task #342). For zero-downtime rotation, set the new value as `BOT_SERVICE_TOKEN` and (optionally) keep the prior value in `BOT_SERVICE_TOKEN_PREVIOUS` — `lib/bot-token.ts:getValidTokens()` accepts both during a rollover window. To rotate: generate a new 32-byte hex token, set `BOT_SERVICE_TOKEN_PREVIOUS = <old>` and `BOT_SERVICE_TOKEN = <new>` as Secrets, redeploy, then once all bot callers are on the new value, delete `BOT_SERVICE_TOKEN_PREVIOUS`. Any token ever committed to `.replit` should be considered permanently compromised regardless of subsequent removal.

## State model
- **Plan of record:** `docs/architecture/state-hierarchy-v1.md` — invoice as the noun that moves through 7 phases; claims as work items with per-phase dispositions. Replaces the cancelled Task #512 lock-down plan.
- **Predecessors:** `docs/architecture/invoice-terminal-state.md` (terminal-state contract), `docs/architecture/state-vocabularies-audit.md` (drift census), `docs/architecture/state-migration-plan.md` (cancelled — kept for the row census).
- **Wave 0.5 catalogue:** `docs/architecture/state-wave-0.5-catalogue.md` — full writer/reader inventory + audit hooks A1-A7 + the §8 wave-by-wave delivery log.
- **Wave A packages (shipped 2026-05-06, infrastructure-only, no behavior change):**
  - `@workspace/observability` (`lib/observability/`) — `TransitionActor` discriminated union (user vs system), 32-tag `TRANSITION_SOURCES` enum, 80-name `AUDIT_ACTION_NAMES` enum, and the `SOURCE_TO_ACTION_TABLE` registry that Wave D will wire into every audit-log insert.
  - `@workspace/vocab` additions — `INVOICE_PHASES` (7-tuple), `CLAIM_DISPOSITIONS` (22-tuple), full glossary entries, and `VALID_DISPOSITIONS_BY_PHASE` (the structural cross-row contract that backs the Postgres `validate_disposition_against_phase()` trigger added in Wave B).
  - `@workspace/invoice-state` (`lib/invoice-state/`) — pure derivation helpers `derivePhaseFromLegacy` / `deriveDispositionFromLegacy` implementing the §6 mapping. Used by Wave B's migration backfill and Wave C's read swap.
- **Wave B schema dual-write (shipped 2026-05-07, no reader/writer changes yet):** Migration `0034_invoice_phase_and_disposition.sql` adds the `invoice_phase` + `claim_disposition` Postgres enums, `invoice_groups.phase` + `phase_entered_at`, `claims.disposition`, the `validate_disposition_against_phase` deferrable AFTER trigger that enforces `VALID_DISPOSITIONS_BY_PHASE` row-by-row, three reader indexes (`invoice_groups_phase_idx`, `claims_disposition_idx`, `claims_invoice_group_disposition_idx`), and a one-shot backfill that runs `derivePhaseFromLegacy` / `deriveDispositionFromLegacy` in SQL inside the same transaction as the column adds — atomic, idempotent, with the trigger created **after** the bulk UPDATEs. Three lockstep invariants are guarded automatically: (1) PG enum value lists ↔ TS literal unions in `lib/vocab/src/` by `scripts/src/__tests__/enum-parity.test.ts`; (2) Drizzle schema ↔ generated SQL by the existing `schema-drift` workflow; (3) SQL backfill CASE ↔ TS derivers by `scripts/src/check-invoice-state-derivation.ts` (read-only audit run after migrate). The matching rollback lives at `lib/db/migrations/rollback/0034_invoice_phase_and_disposition.down.sql` (intentionally outside `migrations/` so the runner ignores it). Wave C readers and Wave D writers ship next; until then `phase`/`disposition` are read-only and frozen at backfill time. SSE `ClaimEvent`/`GroupEvent` carry optional `disposition?` / `phase?` fields (additive, populated starting Wave D).

## External Dependencies
- **PostgreSQL:** Primary relational database.
- **Anthropic Claude:** AI for SOP analysis, dispute note generation, and email generation.
- **Playwright:** Browser automation for interacting with the MAS Transportation Provider Support Portal.
- **Google Cloud Storage (GCS):** Object storage for evidence files.
- **Microsoft Outlook (Graph API):** For sending daily brief emails and tracking payor responses (with SMTP fallback).
- **Replit Auth:** OpenID Connect for user authentication.
